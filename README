pip install scipy
python -m pip install rich
pip install schemdraw

TODO

- create env
- use numpy
- use complex numbers (everywhere)
- use python naming convention (underscore in name)
- add benchmark
